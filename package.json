{
  "private": true,
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "A little worker that could... redirect you to the developers, developers, developers on Cloudflare.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "cookie": "^0.5.0"
  }
}
